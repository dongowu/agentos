gate_policy: unanimous
arbiter_policy: two_round
merge_policy: strict
merge_auto_rework: false
max_merge_retries: 1
role_failover: false
max_role_attempts: 2
team_topology: single
max_parallel_teams: 1
role_instances:
  coder:
    - coder.primary
    - coder.backup
